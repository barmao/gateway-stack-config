# Applied to ALL services (non-secret defaults only).
platform:
  name: edge-stack
  managed-by: config-server
  # Bump this and POST /actuator/busrefresh to see hot-refresh propagate.
  config-version: "2"
