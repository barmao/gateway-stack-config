# Applied to ALL services, ALL environments (env-specific values live in
# application-<env>.yml, which the Config Server layers on top per profile).
platform:
  name: edge-stack
  managed-by: config-server
  env: unknown
  config-version: "0"
